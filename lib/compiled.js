"use strict";

require("core-js/modules/es.promise.js");
// summon("quillAndInk");

const thyFuture = undefined;
const romeoDothLive = false;
const truth = "parting is such sweet sorrow";
while (thyFuture === undefined) {
  const knowledge = null;
  console.log(knowledge);
  break;
}
function edify(name) {
  if (name === null || name === undefined) {
    console.error("Beware, my Lord! Thou must provideth a name");
  }
  return (truth);
}
assert(romeoDothLive === false);
function endeavour() {
  return new Promise((resolve, reject) => {
    const info = "Tidings from yonder";
    if (info) {
      resolve(info);
    } else {
      reject(new Error("Alas, the endeavour hath proved but futile"));
    }
  });
}
console.warn("Beware, for errors doth lurk in the shadows...");
try {
  edify(null);
  endeavour();
} catch (error) {
  console.error("Fie! A mishap hath occurred: " + error.message);
}
